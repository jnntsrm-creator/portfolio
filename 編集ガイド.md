# このポートフォリオの編集ガイド

このサイトは **index.html / style.css / app.js の3ファイルだけ** で動きます。
特別なソフトのインストールは不要です。ブラウザとテキストエディタ（メモ帳でも可、
おすすめは無料の [Visual Studio Code](https://code.visualstudio.com/)）があれば編集できます。

---

## 1. まず表示してみる

`index.html` をダブルクリックすると、ブラウザでサイトが開きます。
（デザイン部品をインターネット経由で読み込むので、**ネット接続が必要**です）

---

## 2. 文章・写真を変える（いちばんよく使う）

**編集するのは `app.js` の上のほうにある `SITE_DATA` という部分だけ** です。
ここのテキストや画像URLを書き換えて保存 → ブラウザを更新すれば反映されます。

### 例：自己紹介の文章を変える
```js
profile: {
  nameEn: "SAITO EIKA",                 // ← 英語の名前
  nameJa: "斎藤 叡華 — 地域協働学部4年",  // ← 日本語の名前
  avatar: "images/avatar.jpg",          // ← プロフィール写真（images フォルダの中）
  bio: "ここに自己紹介文が入ります…",   // ← この「"」で囲まれた部分を書き換える
```

### 例：Work（制作物）の1件を変える
```js
{
  id: "w1", no: "01", title: "SUMMER SCHOOL",  // ← タイトル
  place: { label: "SWITZERLAND", x: 250, y: 250 }, // ← 地名（x,yは地球儀上の位置）
  description: "入学直後の夏、スイスへ。",    // ← カードの短い説明
  detail: "詳しい説明はここ…",                 // ← クリックで開く詳細文
  image: "images/w1.jpg",                      // ← メインの写真（images フォルダの中）
  gallery: ["images/w1a.jpg", "images/w1b.jpg"], // ← 詳細画面の写真
  ...
}
```

### 写真を差し替えるには
写真はすべて `portfolio/images/` フォルダに入っています（`avatar.jpg` はプロフィール、
`w1.jpg`〜`w8.jpg` は各Workのメイン写真、`w1a.jpg`/`w1b.jpg` などはギャラリー）。
- **同じ写真を差し替えるだけ**なら、`images` フォルダの中の同じ名前のファイルを
  新しい写真で上書きするのが一番かんたんです（`w1.jpg` を新しい `w1.jpg` に置き換える）。
- 新しく写真を追加したい場合は、写真を `images` フォルダに入れて
  `image: "images/myphoto.jpg"` のようにファイル名を書きます。
- ネット上の写真URL（`"https://…"`）をそのまま貼ることもできます。

> ⚠️ 注意：`"` （ダブルクォート）や `,` （カンマ）は消さないでください。
> 文章の中身だけを書き換えるのがコツです。心配なら1か所ずつ変えて、
> 都度ブラウザを更新して確認しましょう。

---

## 3. 地球儀のピンの位置を動かす

各Workの `place: { label, x, y }` の **x, y** が地球儀上のピン位置です。
- x を大きく → 右へ、小さく → 左へ
- y を大きく → 下へ、小さく → 上へ
- だいたい 130〜670 の範囲が球面の中です（中心は 400,400）。

---

## 4. こまったら

書き換えて表示が崩れたら、**変更を元に戻せば直ります**。
不安な場合は、編集前に `app.js` をコピーしてバックアップを取っておくと安心です。
